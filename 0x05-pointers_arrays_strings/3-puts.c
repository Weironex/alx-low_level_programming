#include "main.h"
#include <stdio.h>

/**
 * _puts - Prints a string, followed by a new line.
 * @str: Pointer to the string
 *
 * Return: Always 0
 */

void _puts(char *str)
{

	printf("%s\n", str);
}
